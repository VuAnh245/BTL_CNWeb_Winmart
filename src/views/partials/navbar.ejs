<nav class="main-navbar navbar navbar-expand-lg">

    <div class="container">

        <!-- Mobile Toggle -->
        <button
            class="navbar-toggler custom-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNav"
            aria-controls="mainNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <i class="bi bi-list"></i>
        </button>

        <!-- Navbar -->
        <div
            class="collapse navbar-collapse"
            id="mainNav"
        >

            <!-- Category Dropdown -->
            <div class="dropdown me-3">
                <button class="cat-btn" type="button" data-bs-toggle="dropdown">
                    <i class="bi bi-list" style="font-size: 1.2rem;"></i>
                    Danh mục sản phẩm
                </button>
                <ul class="dropdown-menu cat-dropdown-menu">
                    <% if (typeof categories !== 'undefined' && categories.length > 0) { %>
                        <% categories.forEach(cat => { %>
                            <li>
                                <a class="dropdown-item cat-dropdown-item" href="/products?category=<%= cat.DanhMucId %>">
                                    <%= cat.TenDanhMuc %>
                                </a>
                            </li>
                        <% }) %>
                    <% } else { %>
                        <li><span class="dropdown-item">Chưa có danh mục</span></li>
                    <% } %>
                </ul>
            </div>

            <!-- Menu -->
            <ul class="navbar-nav navbar-menu me-auto">

                <li class="nav-item">
                    <a class="nav-link active" href="/">
                        <i class="bi bi-house-door"></i>
                        <span>Trang chủ</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="/products">
                        <i class="bi bi-grid"></i>
                        <span>Sản phẩm</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="/promotions">
                        <i class="bi bi-gift"></i>
                        <span>Khuyến mãi</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="/contact">
                        <i class="bi bi-headset"></i>
                        <span>Liên hệ</span>
                    </a>
                </li>

            </ul>

            <!-- Right Actions: Additional links like real WinMart -->
            <div class="navbar-actions d-none d-lg-flex gap-4" style="font-weight: 500; font-size: 0.95rem; color: var(--wmart-text-light);">
                <a href="#" style="color: inherit; text-decoration: none;"><i class="bi bi-newspaper me-1"></i> Tin tức WinMart</a>
                <a href="#" style="color: inherit; text-decoration: none;"><i class="bi bi-headset me-1"></i> Tư vấn mua hàng</a>
            </div>

        </div>

    </div>

</nav>